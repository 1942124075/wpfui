<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:controls="clr-namespace:Wpf.Ui.Controls"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <ResourceDictionary.MergedDictionaries>
        <ResourceDictionary Source="pack://application:,,,/Wpf.Ui;component/Controls/NavigationView/NavigationViewItemDefaultStyle.xaml" />
    </ResourceDictionary.MergedDictionaries>

    <ControlTemplate TargetType="{x:Type controls:NavigationViewItem}" x:Key="LeftMinimalCompactNavigationViewItemTemplate">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>
            <Border
                Background="{DynamicResource NavigationViewItemBackground}"
                BorderBrush="{TemplateBinding BorderBrush}"
                BorderThickness="1"
                CornerRadius="{TemplateBinding Border.CornerRadius}"
                Grid.Row="0"
                Margin="0"
                MinHeight="40"
                MinWidth="40"
                Padding="0"
                x:Name="MainBorder">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="40" />
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>

                    <Grid Grid.Column="0">
                        <ContentControl
                            Content="{TemplateBinding Icon}"
                            Foreground="{DynamicResource NavigationViewItemForeground}"
                            HorizontalAlignment="Center"
                            Margin="-1,0,0,0"
                            VerticalAlignment="Center"
                            x:Name="IconContentPresenter" />
                    </Grid>

                    <Rectangle
                        Fill="{DynamicResource NavigationViewSelectionIndicatorForeground}"
                        Grid.Column="0"
                        Height="16"
                        HorizontalAlignment="Left"
                        Margin="0"
                        Opacity="0.0"
                        RadiusX="2"
                        RadiusY="2"
                        VerticalAlignment="Center"
                        Width="3"
                        x:Name="ActiveRectangle" />

                    <ContentPresenter
                        Content="{TemplateBinding Content}"
                        ContentTemplate="{TemplateBinding ContentTemplate}"
                        Grid.Column="1"
                        HorizontalAlignment="Left"
                        RecognizesAccessKey="True"
                        TextElement.FontSize="14"
                        TextElement.Foreground="{TemplateBinding Foreground}"
                        VerticalAlignment="Center"
                        x:Name="ElementContentPresenter" />

                    <ContentPresenter
                        Content="{TemplateBinding InfoBadge}"
                        Grid.Column="2"
                        Margin="0"
                        x:Name="PART_InfoBadge" />

                    <controls:SymbolIcon
                        Grid.Column="3"
                        Symbol="ChevronDown24"
                        Visibility="Collapsed"
                        x:Name="ChevronIcon" />
                </Grid>
            </Border>
            <ItemsControl
                Focusable="False"
                Grid.Row="1"
                ItemsSource="{TemplateBinding MenuItemsSource}"
                KeyboardNavigation.DirectionalNavigation="Contained"
                ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                ScrollViewer.VerticalScrollBarVisibility="Disabled">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <StackPanel IsItemsHost="True" Margin="0" />
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.Resources>
                    <Style BasedOn="{StaticResource NavigationViewItemDefaultStyle}" TargetType="{x:Type controls:NavigationViewItem}">
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="{x:Type controls:NavigationViewItem}">
                                    <Border
                                        Background="Transparent"
                                        BorderBrush="{TemplateBinding BorderBrush}"
                                        BorderThickness="1"
                                        CornerRadius="{TemplateBinding Border.CornerRadius}"
                                        Grid.Row="0"
                                        Margin="0"
                                        MinHeight="40"
                                        Padding="0"
                                        x:Name="MainBorder">
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="40" />
                                                <ColumnDefinition MinWidth="180" Width="*" />
                                                <ColumnDefinition Width="Auto" />
                                                <ColumnDefinition Width="Auto" />
                                            </Grid.ColumnDefinitions>

                                            <Grid Grid.Column="0">
                                                <ContentPresenter
                                                    Content="{TemplateBinding Icon}"
                                                    HorizontalAlignment="Center"
                                                    Margin="-1,0,0,0"
                                                    TextElement.Foreground="{DynamicResource NavigationViewItemForeground}"
                                                    VerticalAlignment="Center"
                                                    x:Name="IconContentPresenter" />
                                            </Grid>

                                            <Rectangle
                                                Fill="{DynamicResource NavigationViewSelectionIndicatorForeground}"
                                                Grid.Column="0"
                                                Height="16"
                                                HorizontalAlignment="Left"
                                                Margin="0"
                                                Opacity="0.0"
                                                RadiusX="2"
                                                RadiusY="2"
                                                VerticalAlignment="Center"
                                                Width="3"
                                                x:Name="ActiveRectangle" />

                                            <ContentPresenter
                                                Content="{TemplateBinding Content}"
                                                ContentTemplate="{TemplateBinding ContentTemplate}"
                                                Grid.Column="1"
                                                HorizontalAlignment="Left"
                                                RecognizesAccessKey="True"
                                                TextElement.FontSize="14"
                                                TextElement.Foreground="{TemplateBinding Foreground}"
                                                VerticalAlignment="Center"
                                                x:Name="ElementContentPresenter" />

                                            <ContentPresenter
                                                Content="{TemplateBinding InfoBadge}"
                                                Grid.Column="2"
                                                Margin="0"
                                                x:Name="PART_InfoBadge" />

                                            <controls:SymbolIcon
                                                Grid.Column="3"
                                                Symbol="ChevronDown24"
                                                Visibility="Collapsed"
                                                x:Name="ChevronIcon" />
                                        </Grid>
                                    </Border>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </ItemsControl.Resources>
            </ItemsControl>
        </Grid>
        <ControlTemplate.Triggers>
            <Trigger Property="IsPaneOpen" Value="True">
                <Setter Property="Grid.Column" TargetName="PART_InfoBadge" Value="2" />
            </Trigger>
            <Trigger Property="IsPaneOpen" Value="False">
                <Setter Property="Grid.Column" TargetName="PART_InfoBadge" Value="0" />
                <Setter Property="VerticalAlignment" TargetName="PART_InfoBadge" Value="Top" />
                <Setter Property="HorizontalAlignment" TargetName="PART_InfoBadge" Value="Right" />
            </Trigger>

            <Trigger Property="HasMenuItems" Value="True">
                <Setter Property="Visibility" TargetName="ChevronIcon" Value="Visible" />
                <Setter Property="Margin" TargetName="ChevronIcon" Value="8,0,8,0" />
            </Trigger>
            <Trigger Property="IsActive" Value="True">
                <Setter Property="Opacity" TargetName="ActiveRectangle" Value="1.0" />
                <Setter Property="Background" TargetName="MainBorder" Value="{DynamicResource NavigationViewItemBackgroundSelected}" />
                <!--<Setter TargetName="IconContentPresenter" Property="Filled" Value="True" /> -->
                <!--<Setter TargetName="IconContentPresenter" Property="Foreground" Value="{DynamicResource NavigationViewSelectionIndicatorForeground}" />-->
            </Trigger>
            <Trigger Property="Icon" Value="{x:Null}">
                <Setter Property="Visibility" TargetName="IconContentPresenter" Value="Collapsed" />
            </Trigger>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Background" TargetName="MainBorder" Value="{DynamicResource NavigationViewItemBackgroundPointerOver}" />
            </Trigger>
            <MultiTrigger>
                <MultiTrigger.Conditions>
                    <Condition Property="IsMouseOver" Value="True" />
                    <Condition Property="IsActive" Value="False" />
                </MultiTrigger.Conditions>
                <MultiTrigger.Setters>
                    <Setter Property="Foreground" TargetName="IconContentPresenter" Value="{DynamicResource NavigationViewItemForeground}" />
                    <Setter Property="TextElement.Foreground" TargetName="ElementContentPresenter" Value="{DynamicResource NavigationViewItemForeground}" />
                </MultiTrigger.Setters>
            </MultiTrigger>
        </ControlTemplate.Triggers>
    </ControlTemplate>
</ResourceDictionary>